# Database Schema

This directory contains the SQL scripts necessary to initialize the database structure and load initial test data.

- `schema.sql` : Script de création des tables et insertion des données de test.
